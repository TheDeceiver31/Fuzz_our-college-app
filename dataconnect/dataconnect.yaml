specVersion: "v1beta"
serviceId: "fuzz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fuzz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
